<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="48dp"
    android:background="?android:attr/selectableItemBackground"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingLeft="4dp"
    android:paddingRight="8dp">

    <!-- J-Card Cassette Formulation Spine (Type IV Gold, Type II Blue, Type I Grey) -->
    <View
        android:id="@+id/vTapeSpine"
        android:layout_width="3.5dp"
        android:layout_height="match_parent"
        android:layout_marginTop="5dp"
        android:layout_marginBottom="5dp"
        android:background="@color/brand_orange" />

    <!-- Track Index / Number -->
    <TextView
        android:id="@+id/tvTrackIndex"
        android:layout_width="28dp"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:gravity="center"
        android:text="01"
        android:textColor="@color/lite_text_muted"
        android:textSize="11sp" />

    <!-- Title & Artist -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingLeft="8dp"
        android:paddingRight="8dp">

        <TextView
            android:id="@+id/tvItemTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Song Title"
            android:textColor="@color/lite_text_primary"
            android:textSize="13sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvItemArtist"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Artist Name • Album"
            android:textColor="@color/lite_text_secondary"
            android:textSize="10sp" />
    </LinearLayout>

    <!-- Format & Duration -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="end"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvItemFormat"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:text="FLAC"
            android:textColor="@color/brand_orange"
            android:textSize="9sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvItemDuration"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:text="03:45"
            android:textColor="@color/lite_text_muted"
            android:textSize="10sp" />
    </LinearLayout>

</LinearLayout>
